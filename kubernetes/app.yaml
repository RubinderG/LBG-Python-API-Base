apiVersion: v1
kind: Pod
metadata:
  name: api-app
  labels:
    app: api
spec:
  containers:
  - name: api-container
    image: rubinder/api:latest
---
apiVersion: v1
kind: Service
metadata:
  name: api-app-service
spec:
  type: LoadBalancer
  selector:
    app: api
  ports:
  - protocol: TCP
    port: 80 
    targetPort: 8080
